# Demo Client Module

The demo client owns a plain static website. The current request is to edit a services
page without adding third-party tracking or a hosted page builder.

Useful sources:

- `../../sources/demo-client-request.md`

Acceptance:

- The answer should name the source that justifies the next action.
- The implementation should remain portable plain files.

