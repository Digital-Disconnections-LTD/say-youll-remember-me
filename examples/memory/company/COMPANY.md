# Demo Company Memory

Standing rule: public outputs must be source-linked and portable.

For customer or project work, load only the active module plus company baseline.
Do not load unrelated modules just because they exist.

